# Copy this file to .env, configure both values, and never commit real credentials.
OPENAI_API_KEY=replace-with-your-openai-api-key
# Generate with: openssl rand -hex 32
ANALYSIS_API_KEY=
ENVIRONMENT=development
